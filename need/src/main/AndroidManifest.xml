<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mdlawson.need" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:name=".Need"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.mdlawson.need.MainActivity"
            android:label="@string/app_name"
            android:launchMode="singleTop" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
                android:name="com.mdlawson.need.ArticleDetailActivity"
                android:label="@string/title_article_detail"
                android:parentActivityName="com.mdlawson.need.MainActivity" >
            <meta-data
                    android:name="android.support.PARENT_ACTIVITY"
                    android:value="com.mdlawson.need.MainActivity" />
        </activity>
    </application>

</manifest>
